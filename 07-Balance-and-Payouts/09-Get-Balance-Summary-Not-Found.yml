info:
  name: 09 Get Balance Summary Not Found
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/payout_balance_summary/non-existent-uuid-99999"
  headers:
    - name: Authorization
      value: "{{AUTHTOKEN}}"
    - name: data-service
      value: "{{DATASERVICE}}"
    - name: application-id
      value: "{{APPLICATION_ID}}"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return error for non-existent sub-account", function() {
          expect(res.getStatus()).to.be.oneOf([404, 412, 500]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get balance summary for non-existent sub-account.
  Should return 404.
