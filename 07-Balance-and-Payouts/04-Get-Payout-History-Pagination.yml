info:
  name: 04 Get Payout History Pagination
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/get_payout_history/{{SUB_UUID}}?limit=5"
  headers:
    - name: Authorization
      value: "{{AUTHTOKEN}}"
    - name: data-service
      value: "{{DATASERVICE}}"
    - name: application-id
      value: "{{APPLICATION_ID}}"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 200", function() {
          expect(res.getStatus()).to.be.oneOf([200, 500]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get payout history with pagination params.
  Tests limit and cursor-based pagination.
