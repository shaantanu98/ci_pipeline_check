{
  "name": "e2e-api-tests",
  "version": "1.0.0",
  "description": "LeapPay E2E API Tests (Bruno Collection)",
  "main": "index.js",
  "scripts": {
    "test": "bru run --env staging --sandbox developer --output results.json",
    "test:dev": "bru run --env dev --sandbox developer --output results.json",
    "test:staging": "bru run --env staging --sandbox developer --output results.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@faker-js/faker": "^10.4.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@usebruno/cli": "^3.4.0"
  }
}
