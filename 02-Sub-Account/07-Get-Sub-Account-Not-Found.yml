info:
  name: 07 Get Sub Account Not Found
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/sub_account/non-existent-uuid-12345"
  headers:
    - name: Authorization
      value: "{{AUTHTOKEN}}"
    - name: data-service
      value: "{{DATASERVICE}}"
    - name: application-id
      value: "{{APPLICATION_ID}}"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 404", function() {
          expect(res.getStatus()).to.be.oneOf([200, 404, 412]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get sub-account with non-existent UUID.
  Should return 404 error.
