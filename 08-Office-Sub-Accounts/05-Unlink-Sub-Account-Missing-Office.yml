info:
  name: 05 Unlink Sub Account Missing Office
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/unlink_sub_account_office"
  headers:
    - name: Authorization
      value: "{{AUTHTOKEN}}"
    - name: data-service
      value: "{{DATASERVICE}}"
    - name: application-id
      value: "{{APPLICATION_ID}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {}
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return error for missing office_id", function() {
          expect(res.getStatus()).to.be.oneOf([400, 412, 500]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Validation: office_id is required.
  Should return 400 when missing.
