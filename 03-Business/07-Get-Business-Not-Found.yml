info:
  name: 07 Get Business Not Found
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/business/non-existent-uuid-99999"
  headers:
    - name: Authorization
      value: "{{AUTHTOKEN}}"
    - name: data-service
      value: "{{DATASERVICE}}"
    - name: application-id
      value: "{{APPLICATION_ID}}"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 404", function() {
          expect(res.getStatus()).to.be.oneOf([200, 404, 412]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get business with non-existent UUID.
  Should return 404.
