info:
  name: 04 Get Company Rate Not Found
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/leappay_company_rate/nonexistent_company_99999"
  headers:
    - name: Authorization
      value: "{{AUTHTOKEN}}"
    - name: data-service
      value: "{{DATASERVICE}}"
    - name: application-id
      value: "{{APPLICATION_ID}}"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return error or default rate", function() {
          expect(res.getStatus()).to.be.oneOf([200, 404, 412]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get rate for non-existent company ID.
  Should return 404 or default rate.
