info:
  name: 12 Get Payment Method Not Found
  type: http
  seq: 12

http:
  method: GET
  url: "{{BASE_URL}}/sub_account/{{SUB_UUID}}/payment_method/pm_invalid_token_99999"
  headers:
    - name: Authorization
      value: "{{AUTHTOKEN}}"
    - name: data-service
      value: "{{DATASERVICE}}"
    - name: application-id
      value: "{{APPLICATION_ID}}"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return error for invalid token", function() {
          expect(res.getStatus()).to.be.oneOf([400, 404, 412, 500]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get payment method with invalid token.
  Should return error.
