info:
  name: 08 Create Payment Sub Account Not Found
  type: http
  seq: 8

http:
  method: POST
  url: "{{BASE_URL}}/payment/:uuid"
  params:
    - name: uuid
      value: non-existent-uuid-99999
      type: path
  headers:
    - name: Authorization
      value: "{{AUTHTOKEN}}"
    - name: data-service
      value: "{{DATASERVICE}}"
    - name: application-id
      value: "{{APPLICATION_ID}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
      "amount": 10000,
      "payment_token": "pm_test"
      }
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 404 for non-existent sub-account", function() {
          expect(res.getStatus()).to.be.oneOf([404, 412]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Error: payment on non-existent sub-account.
  Should return 404.
