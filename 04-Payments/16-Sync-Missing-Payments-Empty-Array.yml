info:
  name: 16 Sync Missing Payments Empty Array
  type: http
  seq: 16

http:
  method: POST
  url: "{{BASE_URL}}/sync-missing-payments"
  headers:
    - name: Authorization
      value: "{{AUTHTOKEN}}"
    - name: data-service
      value: "{{DATASERVICE}}"
    - name: application-id
      value: "{{APPLICATION_ID}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
      "payment_ids": []
      }
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return error for empty array", function() {
          expect(res.getStatus()).to.be.oneOf([400, 412]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Validation: payment_ids must be non-empty array.
  Should return error for empty array.
